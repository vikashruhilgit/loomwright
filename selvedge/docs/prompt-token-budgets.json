{
  "_comment": "Authoritative per-agent prompt-inventory budgets for the SELVEDGE plugin. Budgets are PER PLUGIN — never shared with loomwright. Enforced by scripts/check-token-budget.sh (fail-closed) and mirrored for humans in docs/ARCHITECTURE_CONTRACTS.md §'Prompt Token Budgets'; the gate checks BOTH the budget and the measured cell against this file, so the two cannot drift.",
  "proxy_bytes_per_token": 4,
  "agents": {
    "qa-executor": {
      "budget": 48222,
      "measured": 43838,
      "note": "measured 43838 + ~10% at the selvedge relocation. RE-MEASURED FROM THE POST-MOVE FILES, deliberately not carried over from loomwright's 43786: the same PR renames the frontmatter `name:`, both debate-loop spawn targets and the agent-memory writer sentence, so a weight taken before those edits is stale by construction. 5 preloaded skills — 4 selvedge-local plus `quality-checklist`, which stays LOOMWRIGHT-OWNED and resolves cross-plugin (the gate attributes it as `quality-checklist@loomwright` in the DETAIL column)."
    },
    "qa-strategist": {
      "budget": 23540,
      "measured": 21400,
      "note": "measured 21400 + ~10% at the selvedge relocation. Re-measured from the post-move file for the same reason as qa-executor (previously 21352 under loomwright). 3 preloaded skills — 2 selvedge-local plus the cross-plugin `quality-checklist@loomwright`."
    }
  }
}
