# Automate Run: twin-remediation folder
## Status: done
## Source
- folder .supervisor/requirements/twin-remediation
## Run Config
- mode: safe | limit: 3 | trust_unprotected: false
- auto_review_original: absent | config_backup: automate-2026-07-23-161331.config-backup.json
## Queue
- [x] .supervisor/requirements/twin-remediation/00-overview.md  # skipped: index/policy doc (sequencing + advisory-surface freeze), no acceptance criteria to implement
- [x] .supervisor/requirements/twin-remediation/02-curation-anti-rot.md
- [x] .supervisor/requirements/twin-remediation/07-parity-ablation-eval.md  # abandoned: REDUCED to final-state items 05+06 (goal file: not finishing the 5-requirement corpus as-is); 1 of 5 corpus entries produced a real verdict (SDK runner = CUT)
- [x] .supervisor/requirements/twin-remediation/03-mechanize-rules-tier1.md  # abandoned: carried forward UNHOMED - not superseded, not present in final-state/; re-enqueue explicitly when scheduled (00-overview: 'unaffected')
- [x] .supervisor/requirements/twin-remediation/04-claude-md-diet.md  # abandoned: carried into final-state/09-claude-md-diet-dreaming.md (extended with /dreaming hook)
- [x] .supervisor/requirements/twin-remediation/05-orchestration-simplification-audit.md  # abandoned: SUPERSEDED - the eval produced the verdicts this item was meant to propose; final-state 01+04 are the executable form
- [x] .supervisor/requirements/twin-remediation/06-derived-artifact-freshness.md  # abandoned: carried forward UNHOMED - partially overlaps final-state/07 (4g); reconcile at that item's PLAN, do not duplicate
## Current
- item: .supervisor/requirements/twin-remediation/07-parity-ablation-eval.md | status: escalated | pr: https://github.com/vikashruhilgit/loomwright/pull/107 (MERGED e11e04c — prep-only scope) | branch: feature/parity-eval-prep (merged)
- pause_reason: null   # archived: queue fully resolved (5 abandoned w/ reasons); superseded by .supervisor/requirements/final-state/
- owned_drain_started: true | owned_drain_result: READY | suppressed_default_dispatch: true
## Progress
- 2026-07-23 16:13:31 run created (folder source, 7 resolved / 6 processable, limit 3, safe mode). 01-prove-the-loop.md excluded by resolve-folder (## Status: done, PR #105 merged); 00-overview.md enqueued pre-skipped as an index doc.
- 2026-07-23 16:13:31 RESUME reconcile at start: 2 prior incomplete runs found. token-economy (automate-2026-07-14-151145): PR #104 MERGED f55380b -> item 05 checked off, pause_reason escalated -> limit_reached, item 06 remains. review-remediation (automate-2026-07-06-100629): paused, no open PR, item 10 remains. Zero open PRs -> single-open-PR invariant (§8) clear.
- 2026-07-23 16:33:20 queue REORDERED (user-confirmed): 07-parity-ablation-eval moved upstream of 03/04/05. Rationale: 07's own Goal states its per-layer keep/cut verdicts 'feed items 03-05', so 03/04/05 would otherwise prune/keep layers without the pre-registered experimental evidence (00-overview's numeric sequencing contradicts 07's own text). 02 stays first: it is the advisory-surface freeze-lifter (00-overview condition b) and is independent of 07 (its non-goals defer store consolidation to item 01's verdicts, not 07's). 07 itself does not trip the freeze (verdicts-only, no new advisory reader/store/emitter). Order: 02 -> 07 -> 03 -> 04 -> 05 -> 06; limit 3 covers 02/07/03.
- 2026-07-23 16:33:20 queue CONFIRMED by user; picking 02-curation-anti-rot.md (safe mode).
- 2026-07-23 16:59:54 item 02 PLAN complete: Launch Pad inline, Plan Review PASS attempt 3/3 (FAIL,FAIL,PASS). Brief saved .supervisor/jobs/pending/2026-07-23-curation-anti-rot.md (6 subtasks, 3 LAUNCHABLE, workers 3). Rounds caught unimplementable pinned encodings: write-lessons.sh already ships retraction; read-orientation.sh greedy areas capture would silently disable staleness; round-2 add-then-retract would destroy an unrelated lesson via evict-oldest-from-front (write-lessons.sh:331-341) -> re-pinned PRE-CHECK/RETRACT/ADD. Corrected stale dreaming.md:86 claim (read-lessons.sh has a live consumer at supervisor.md:140).
- 2026-07-23 17:29:29 --cheap ELECTED mid-run by user at the EXECUTE gate (was NOT passed at run creation). Per automate-loop SKILL.md §11 --cheap is passthrough-only and is deliberately NOT persisted in ## Run Config -> RE-PASS '--cheap' on EVERY /automate --resume and /loop tick of this run, or the remaining queue silently reverts to the full-cost profile. Forwarding to /supervisor for item 02.
- 2026-07-23 17:35:30 item 02 ACQUIRE: branch feature/curation-anti-rot; brief moved pending->in-progress. Phase 1.5 PRE-FLIGHT SYNC = OVERLAP (not SUPERSEDED): merged PR #98 / ddb73f2 (v15.7.0 'corpus curation half') already shipped curate-postmortem.sh retract+supersede, write-lessons.sh retract, read-lessons.sh staleness+RETRACTED, build-insights.sh ## Corpus health. User chose revise-scope. Brief amended with a '## Prior art' section: drops already-shipped work, and pins per-file verb shapes (rules/orientation follow curate-postmortem.sh --target/--reason/--replacement-required-on-supersede/--confirm; lessons mirrors write-lessons.sh's own positional/--hash retract shape + required --replacement). NOTE: the amendment did NOT pass Plan Review (3/3 cap exhausted at PLAN) - flag for extra scrutiny at Phase 4.5.
- 2026-07-23 17:47:42 ST-3 (lessons) complete: write-lessons.sh supersede verb (PRE-CHECK->RETRACT->ADD), +240/-31 across write-lessons.sh + test-lessons.sh + test-read-lessons.sh. test-lessons.sh 69/69 PASS incl. the MANDATORY eviction regression (full 3-entry category, supersede middle, oldest SURVIVES) and byte-identical refusal on absent/untrusted target. NOTE: worker hit its turn limit before emitting WORKER_RESULT / .worker-summary.md - outcome verified directly by the Supervisor by running the tests, not inferred from the missing block.
- 2026-07-23 17:51:29 ST-1 + ST-2 implementations landed (ST-1 add-rule.sh +249/read-rules.sh +59, existing suites 47/47 + 29/29; ST-2 read-orientation.sh +178/add-orientation.sh +261, existing suites 11/11 + 13/13) but BOTH hit the worker turn limit before writing new test cases - same failure mode as ST-3. DECOMPOSITION FINDING: all 3 subtasks (reader + writer + 2-4 test files each) exceeded the worker turn budget; size future store-curation subtasks smaller or split impl/tests. Both workers resumed via SendMessage to finish tests only. INTEGRATION FINDING for Phase 4.5: ST-1 and ST-2 shipped DIVERGENT interfaces for the same concept - ST-1 uses '--supersedes' as an optional flag on add + '--retract' as a separate action with no --replacement; ST-2 uses '--supersedes --target <old> --replacement <new>' as a distinct action. Neither worker could see the other's worktree. Reconcile at the holistic review.
- 2026-07-23 17:54:09 ST-2 (orientation) COMPLETE + verified by Supervisor: 18/18 reader + 21/21 writer, incl. the MANDATORY staleness-with-supersedes regression; only the 4 assigned files touched; outputs_gap empty. Fixed the greedy-capture header parse via per-key sed extraction (legacy 3-field + new 4-field both parse). Worker also found a SECOND latent bug unprompted: a tab-delimited temp table silently collapsed the empty supersedes field because POSIX read treats tab as IFS whitespace regardless of custom IFS -> switched to ASCII unit separator 0x1F. Emitted as a memory_candidate for /dreaming promotion (human-gated) rather than hand-written elsewhere.
- 2026-07-23 17:55:47 ST-1 COMPLETE + verified (171/171 across 5 rules suites; found+fixed a real jq bug: index(FILTER) evaluates against the piped array not outer '.'). BATCH 1 MERGED into feature/curation-anti-rot (b7ccf2a): 11 files, +1402/-131, three branches provably disjoint at the pre-merge gate. Integrated-branch re-run: 300/300 tests green across all 9 curation suites. Worktrees removed, subtask branches deleted. Batch 2 dispatched: ST-4 (insights+ledger) + ST-5a (command surfaces) in parallel - disjoint file sets.
- 2026-07-23 18:05:21 Batch 2 in flight. ST-5a: 4/6 deliverables landed (stale dreaming.md read-lessons 'no consumer' claim FIXED and correctly attributed to supervisor.md:140 Phase 1 ACQUIRE Actions step 0 - Supervisor independently verified the phase attribution); resumed for LESSONS curation + harness-pointer subsections. ST-4: ledger advisory_total additive-if-present + Corpus health correctly EXTENDED (no parallel ## Stale knowledge section); test-token-ledger 111/111 green BUT test-insights 138/1 FAILED - ST-4's extension regressed the 'absent corpora degrade gracefully' no-corpora note; resumed with regression as priority 1, plus the unconfirmed advisory_tokens duplicate-measure reconciliation (named brief risk). DECOMPOSITION FINDING UPDATED: 4 of 4 workers so far hit the turn limit, INCLUDING a docs-only subtask with no tests to defer - so the cause is per-subtask work VOLUME, not test-ordering; my batch-2 'write tests incrementally' mitigation addressed the wrong variable. Correct fix is smaller subtasks.
- 2026-07-23 18:06:19 ST-5a COMPLETE + verified: 24/24 test-rules-docs, all 4 outputs present, outputs_gap empty, only its 4 files touched. Documented the three divergent shapes per-store with invocations copied verbatim from the real scripts (verified: add-rule.sh genuinely has NO supersede action, only the flag-on-add - docs match reality). HANDOFF TO ST-5b (load-bearing): ST-5a deliberately did NOT bump skills/rules/SKILL.md frontmatter version/lastUpdated (still 1.1.0 / 2026-07-03) despite substantive content changes, to avoid desyncing SKILLS_INDEX.md's version cell which check-skills-index-sync.sh fails CI on. ST-5b MUST bump both in lockstep. ST-5a follow-up suggestion (out of its scope): normalize write-lessons.sh onto --target with positional as a compat alias, so all four curation surfaces share curate-postmortem.sh's shape.
- 2026-07-23 18:20:38 ST-4 (269/269, regression fixed by restoring the exact degrade string not by weakening the assertion; advisory_tokens duplicate reconciled via column rename + cross-link) and ST-5b (v15.14.0 both manifests, freeze rule -> .agent/rules/process.json readable back via read-rules.sh, SKILL/SKILLS_INDEX lockstep 1.2.0/2026-07-23) COMPLETE. All 6 subtasks merged. FINALIZE: 579/579 tests across 11 suites, 6/6 CI gates PASS, pushed, PR #106 OPENED (base main). Entering Phase 4.5.
- 2026-07-23 18:54:50 Phase 4.5 COMPLETE: holistic review FAIL (3 HIGH: n>2 supersedes cycles silently hid ALL cycle members in read-rules.sh AND read-orientation.sh - root cause was a SPEC GAP in my pinned contract which described only the 2-node case; plus '(no corpora found)' co-rendering with populated decay lines, untested) -> heal iteration 1 -> re-review PASS. heal_decision PASS, heal_iterations 1, rubric_score 4/4. Tests 579->594, 7/7 CI gates. Heal 1's FIRST attempt was worse than the bug (broken jq program routed read-rules.sh down its jq-unavailable fail-safe = hid EVERY rule); caught by per-suite floors before commit. Completion tail done: job -> done/, state completed, requirement 02 stamped '## Status: done', auto_review RESTORED (config.json deleted, original was __ABSENT__; transient backup removed). No dispatch marker for pull/106 -> suppression held, single-drain invariant intact. Owned drain pending claude-review check settling (ci already SUCCESS).
- 2026-07-23 19:04:59 owned drain /review-pr --until-mergeable: 1 fix round (stale absolute line-ref in CHANGELOG -> replaced with a descriptive anchor so it cannot rot again; the bot's other 2 findings predated the heal-1 push and were already fixed - verified rather than reflexively re-fixed). ci + claude-review SUCCESS, 0 unresolved threads, bot verdict no-blocking. drain READY -> awaiting_merge. Learning line emitted (source automate_drain, review_rounds 1, 28 changed_paths, real repo). RUN PAUSED (safe mode, single-open-PR invariant): merge PR #106, then '/loomwright:automate --resume automate-2026-07-23-161331 --cheap' to process 07 and 03. RE-PASS --cheap (not persisted by contract).
- 2026-07-23 22:03:15 RESUME automate-2026-07-23-161331: PR #106 MERGED (c5b860a @ 2026-07-23T16:27:43Z) -> item 02 checked off; main synced to 065f964; remaining 5. --cheap RE-PASSED (elected mid-run for item 02; not persisted by contract). Config backup absent (already restored). Single-open-PR invariant clear.
- 2026-07-23 22:03:15 picking 07-parity-ablation-eval.md (item 2 of limit 3; processed=1 after 02 merge). Suppressing auto_review; invoking /autonomous --single-iteration --cheap --requirement <path>.
- 2026-07-23 22:04:00 RUNFILE RESTORED after a botched runfile-write (empty stdin from a failed python|/tmp redirect wiped the file to Progress-only). Restored from in-session transcript of the pre-wipe content + the two Progress lines that survived. Queue/Current/Status reconstructed to match post-merge reconcile state. Continuing item 07.
- 2026-07-23 22:05:30 item 07 Launch Pad Phase 1 VALIDATE: CLAUDE.md ok, git clean on main@065f964, worktrees clean, gh auth ok, disk ok, source doc committed (gitignored under .supervisor/). 0 blockers / 0 warnings. Phase 2: requirement file loaded (execute FABLE_PARITY_EVAL 5x3 + >=2 ablation arms; verdicts-only). Phase 2.5 FEASIBILITY = CAUTION — surfacing to user before Plan Review / save (15 in-session autonomous runs + ablation arms is multi-day / high-token; cannot be a normal code-change Supervisor job).
- 2026-07-24 07:23:55 RESUME automate-2026-07-23-161331: config.json stranded (auto_review:false from item 07 suppress, backup absent) -> deleted (original absent). Item 07 was mid-Launch-Pad Phase 2.5 CAUTION (no brief, no PR, no branch). Re-picking item 07 fresh. --cheap NOT passed this invocation (user did not re-pass it).
- 2026-07-24 07:40:11 item 07 Launch Pad Phase 2.5 = NO-GO (experiment orchestration, 15+ autonomous sessions can't nest inside a Supervisor job). User chose REVISE SCOPE: reframe to deliverables only (corpus selection + scratch branches + eval runbook) — NOT running the 15 sessions. Looping back to Phase 2 with revised scope.
- 2026-07-24 07:53:48 item 07 PLAN complete: Launch Pad inline (revised scope: deliverables-only), Plan Review PASS attempt 2/3. Brief saved .supervisor/jobs/pending/2026-07-24-parity-eval-prep.md (1 subtask, fast-path, 1 file modified: FABLE_PARITY_EVAL.md). Scope: corpus selection (5 requirements from done jobs) + eval runbook (3 base arms + >=2 ablation arms). NOT running any eval sessions.
- 2026-07-24 08:07:13 item 07 EXECUTE complete: /autonomous + /supervisor inline (fast-path single subtask). PR #107 created (feature/parity-eval-prep, 1 file +188/-0). Phase 4.5 PASS, heal_iterations 1 (fixed arm-3 flag forwarding + ablation numbering gap). Job done/. Config restored (deleted, original absent). Starting owned drain.
- 2026-07-24 08:13:52 owned drain /review-pr --until-mergeable: ci SUCCESS, claude-review SUCCESS. Bot comment found arm-3 + ablation numbering issues — BOTH already fixed in Phase 4.5 heal-1 (commit 15cd4c7). 0 unresolved bot findings. drain READY -> awaiting_merge. RUN PAUSED (safe mode, single-open-PR invariant): merge PR #107, then '/loomwright:automate --resume automate-2026-07-23-161331' to process item 03.
- 2026-07-27 19:08:33 RESUME reconcile: PR #107 MERGED (e11e04c @ 2026-07-27T12:51:19Z) -> item 07's REVISED (prep-only) scope landed; requirement 07 NOT stamped done and stays '- [ ]' because its own acceptance criteria (filled Results table + per-layer verdicts) are unmet. Stranded config-backup restored (config.json originally absent -> deleted, sidecar removed). main synced fc69a4f. Zero open PRs. DECISION (user): finish the 07 EVAL before returning to the queue. Corpus pick = ntfs-tool 03-tree-and-find.md (corpus entry #1, the only 'YES - strongest' dependency carrier: tree+find import a walker a third subtask creates, so arm-3 SDK-runner divergence 3 yields a COMPILE failure, not a subtle diff; verified open at ntfsctl.swift:17). Branches created off 5df1ded: arm-1-bare (local), arm-2-default + arm-3-extras (pushed). FINDING: items 03/04/05 contain ZERO references to the eval/ablations/item 07 - the 'verdicts feed 03-05' claim exists only in 07's own Goal, so the 07-23 reorder was precautionary, not a hard dependency. Automate run stays paused; the eval is operator-run and cannot be driven by /automate (inlines /autonomous, which drops --skip-preflight-sync + --sdk-runner + --multi-voter-heal).
- 2026-07-28 06:47:10 item 07 EVAL PARTIAL — arm 1 complete, arms 2/3 blocked. Corpus entry #1 (ntfs-tool 03-tree-and-find). ARM 1 (bare): commit 4406708, post_merge_defects=0 (reviewer PASS, 0 BLOCKING/0 HIGH), tests green 286+35, 98 turns / 41m / $9.70; row committed to FABLE_PARITY_EVAL.md Results at run time. Baseline cleanliness verified by session-birth-time vs settings.json mtime after the FIRST attempt ran contaminated (plugin loaded; preserved on arm-1-CONTAMINATED-plugin-loaded). ARM 2 NOT COMPLETED: Supervisor merged all 5 subtasks then backgrounded the test suite and ended its turn awaiting re-invocation that 'claude -p' never provides -> no push, no PR, no Phase 4.5; 3 of 4 metrics unobtainable ($43.63). STRUCTURAL: Loomwright arms cannot run headless at all (async dispatch-then-poll needs a re-invoked session) -> arms 2/3 + ablations require interactive sessions the assistant cannot drive (computer-use gives terminals/IDEs click-tier only, no typing). TWO DEFECTS FOUND: (1) Supervisor incompatible with headless one-shot; (2) state.md left at phase ACQUIRE with all subtasks PENDING though all 5 merged -> '--continue' would silently re-execute the whole job; schema-valid but false, so the v15.3.0 fail-closed gate does not catch it. Follow-up requirement written: .supervisor/requirements/supervisor-resume-state-lies.md. Eval results branch eval/results-tree-and-find pushed (fce06c5, 48cbe6e). Session spend $80.49. Item 07 REMAINS unchecked - acceptance criteria (full table + verdicts) unmet; est. $200-500 remaining for all 5 requirements.
- 2026-07-28 08:20:46 item 07 EVAL COMPLETE for corpus entry #1 (ntfs-tool 03-tree-and-find) — 3 arms attempted, ONE REAL VERDICT produced. ARM 1 (bare): post_merge_defects 0, $9.70. ARM 2 (Loomwright): post_merge_defects 0, heal_decision PASS, heal_iterations 0, rubric 6/6, PR #58, $61.76 (6.4x arm 1). ARM 3 (--sdk-runner --multi-voter-heal): ABORTED at Phase 3, $9.59 — SDK runner CANNOT EXECUTE a serial dependency-carrying Launch Pad brief. Two independent blockers verified by reading runner.ts:315-399: (a) brief parser drops all dependency edges (expects numeric ids + subtask_N: YAML keys; Launch Pad emits 1a/1b + comment keys) so everything looked launchable and subtasks 2+3 spawned CONCURRENTLY onto the same file — a SILENT correctness failure, not a crash; (b) no dependency materialization (residual divergence 3) so dependents cannot compile. VERDICT per the pre-committed rule: **SDK RUNNER = CUT**. --multi-voter-heal never ran => UNMEASURED, explicitly NOT cut (rule is per-layer). This RESOLVES item 05's pending hypothesis (b) 'retire or graduate sdk-spike — live verification PENDING'. Metric findings: post_merge_defects had ZERO discriminating power (0/0 both arms — threshold too coarse); wall_tokens under-counts multi-agent arms ~6x (usage object omits sub-agents; cost is the valid comparator). Two Supervisor defects found en route: resume-state-lies (FIXED, PR #110 merged a150c30) and headless-incompatibility (corrected: --resume works, but exit 0 is not a completion signal). PRs #109+#110 MERGED, ntfs-tool #58 CLOSED per isolation protocol. Session spend ~$160. Item 07 REMAINS UNCHECKED — acceptance criteria want all 5 corpus requirements + >=2 ablation arms; 1 of 5 done. Follow-ups: execute the SDK-runner CUT; isolate --multi-voter-heal (~$60); .supervisor/requirements/one-writer-derived-state.md.
- 2026-07-28 (operator note, appended outside /automate) QUEUE SUPERSEDED by .supervisor/requirements/final-state/ (authority: loomwright/docs/SPIKES/FINAL_STATE_GOAL.md, PR #114 merged). Item 05 SUPERSEDED (eval produced its verdicts); item 07 REDUCED to final-state items 05+06; item 04 carried as final-state 09. Items 03/06 survive but should be driven from the final-state queue reconciliation (00-overview.md there). Do NOT resume this run without reading final-state/00-overview.md first.
- 2026-07-28 ARCHIVED (user-confirmed at the start of the final-state run). Queue fully resolved: remaining 5 items checked off as abandoned with per-item reasons (07 REDUCED, 04 CARRIED to final-state/09, 05 SUPERSEDED, 03+06 CARRIED-UNHOMED). Status running->done. NOTE: twin-remediation 03 (mechanize-rules-tier1) and 06 (derived-artifact-freshness) are NOT superseded and have NO home in final-state/ - 06 partially overlaps final-state/07 (4g); 03 needs an explicit re-enqueue. Recorded so they are not silently lost.
