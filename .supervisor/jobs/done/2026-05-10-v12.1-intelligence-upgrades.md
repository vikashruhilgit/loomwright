# Supervisor-Ready Brief: AI Agent Manager v12.1.0 — Intelligence Upgrades

## Environment

- **Project:** ~/Documents/work/AI/ai-agent-manager
- **CLAUDE.md:** ✓ Found (current)
- **Git:** Should be on `main` after PR #8 (v12.0.0) is merged
- **GitHub CLI:** ✓ Authenticated
- **Disk:** Sufficient
- **Blockers:** 0
- **Warnings:** 1 — Spikes S1 and S2 may resolve negative; S5 must accept either outcome

---

## Task

**Goal:** Implement v12.1.0 intelligence upgrades — Memory Tool integration and Structured Outputs guidance unconditionally; Compaction protection and Advisor Tool mode conditionally based on capability spike outcomes. Bump version 12.0.0 → 12.1.0.

**Source plan:** `~/.claude/plans/ok-now-you-have-vectorized-hopcroft.md` (v12.1.0 section)

**Pre-requisite:** PR #8 (v12.0.0) merged to main.

---

## Acceptance Criteria

- [ ] Given Spike A executes, when complete, then `docs/SPIKES/compaction.md` documents whether `PreCompact`/`PostCompact` events support `type: command` handlers, whether `additionalContext`/`systemMessage` injection works post-compaction, and a clear `Recommendation: GO | NO-GO` (no other values permitted)
- [ ] Given Spike B executes, when complete, then `docs/SPIKES/advisor.md` documents whether the Advisor tool is callable from plugin subagents spawned via Task tool (vs. only via direct API), and a clear `Recommendation: GO | SDK-ONLY | NO-GO` (no other values permitted)
- [ ] Given Memory Tool skill creation, when complete, then `skills/memory-tool/SKILL.md` exists with frontmatter (name, description, version, lastUpdated), documents when to use Memory tool vs file-based agent memory, includes per-agent guidance for Red Team / QA Executor / Product Owner; SKILLS_INDEX.md has a new entry
- [ ] Given Structured Outputs guidance, when AGENT_GUIDELINES.md is read, then it contains a "Structured Outputs" section explaining when to enforce result schemas via `output_config.format` at the API level; RESULT_SCHEMAS.md has a note at the top referencing this approach
- [ ] Given Spike A returned GO, when v12.1 implementation is complete, then `hooks/hooks.json` contains `PreCompact` and `PostCompact` hook configurations with `type: command`, `agents/context-keeper.md` documents `flush_for_compaction` operation, `skills/state-management/SKILL.md` and `skills/workflow-management/SKILL.md` have compaction sections
- [ ] Given Spike A returned NO-GO, when v12.1 implementation is complete, then `docs/SPIKES/compaction.md` is the authoritative deferral record and no compaction code is added
- [ ] Given Spike B returned GO, when v12.1 implementation is complete, then `commands/supervisor.md` documents `--advisor` flag, `agents/supervisor.md` parses and propagates `advisor_mode: true` in subtask briefs, `docs/ARCHITECTURE_CONTRACTS.md` Cost Profiles table has an `advisor` row
- [ ] Given Spike B returned SDK-ONLY, when v12.1 implementation is complete, then `AGENT_GUIDELINES.md` documents Advisor as an SDK-only pattern (callable via direct `/v1/messages` API) and no plugin flag is added
- [ ] Given Spike B returned NO-GO, when v12.1 implementation is complete, then no plugin code, no docs, and no `--advisor` flag are added; the spike doc itself is the authoritative deferral record
- [ ] Given all changes complete, when version is checked, then `.claude-plugin/plugin.json` shows `12.1.0`, all README/CLAUDE.md/marketplace.json references are updated, `commands/agent-help.md` count fields are accurate, `skills/SKILLS_INDEX.md` lastUpdated is refreshed
- [ ] Given Memory Tool skill is added (always) and Compaction is implemented (only if Spike A: GO), when count-bearing surfaces are read, then ALL of the following are updated to match: `README.md` (skill count: 48 → 49; hook count: 13 → 15 if Compaction added, else stays 13), `CLAUDE.md` (skill count + hook count + version), `.claude-plugin/README.md` (any count references), `.claude-plugin/plugin.json` `description` field (if it cites counts), `.claude-plugin/marketplace.json` `description` field (if it cites counts), `commands/agent-help.md` (skill count + hook count). The worker MUST grep for the old counts (48, 13) before editing to ensure no surface is missed.
- [ ] Given `scripts/validate-version.sh` and `scripts/check-command-sync.sh` are run, then both pass clean

---

## Subtask Structure

### S1 — Spike A: Compaction Capability Verification
**Goal:** Read-only research to determine whether Claude Code supports `PreCompact`/`PostCompact` hook events with `type: command` handlers, and whether the JSON output (`additionalContext`/`systemMessage` fields) reliably injects context into the resumed session.

**Acceptance criteria:**
- New file `ai-agent-manager-plugin/docs/SPIKES/compaction.md` exists with sections: Question / Sources Consulted / Findings / Recommendation (`GO | NO-GO` — exactly one of these two values, no others) / Implementation Notes (if GO)
- Investigates: Claude Code hooks API docs, changelog mentions of PreCompact/PostCompact, hook handler type matrix, JSON output schema for SubagentStart-like events
- Concludes with `Recommendation: GO` (Spike A confirmed feasible — include JSON output format) or `Recommendation: NO-GO` (deferred indefinitely — spike doc is the deferral record); no `GATED` or other intermediate values
- Read-only — modifies no plugin files

**Files to modify (1 new):**
- NEW `ai-agent-manager-plugin/docs/SPIKES/compaction.md`

**Skill references:** `quality-checklist`

```yaml
provides:
  - {kind: "file", path: "ai-agent-manager-plugin/docs/SPIKES/compaction.md", name: "compaction.md"}
  - {kind: "symbol", path: "ai-agent-manager-plugin/docs/SPIKES/compaction.md", name: "Recommendation"}
requires: []
external_requires:
  - Claude Code hooks API documentation
  - Claude Code changelog for PreCompact/PostCompact mentions
```

---

### S2 — Spike B: Advisor Tool Capability Verification
**Goal:** Read-only research to determine whether the Advisor tool (Opus advisor + Sonnet/Haiku executor) is callable from plugin subagents spawned via Task tool, or only via direct `/v1/messages` API.

**Acceptance criteria:**
- New file `ai-agent-manager-plugin/docs/SPIKES/advisor.md` exists with sections: Question / Sources Consulted / Findings / Recommendation (`GO | SDK-ONLY | NO-GO` — exactly one of these three values, no others) / Implementation Notes (if GO or SDK-ONLY)
- Investigates: Anthropic Advisor tool docs, plugin subagent tool availability, whether `tools` array in Task spawn supports advisor entries
- Concludes with one of: `GO` (implement `--advisor` flag in plugin), `SDK-ONLY` (document as direct-API pattern in AGENT_GUIDELINES.md but no plugin flag), or `NO-GO` (deferred entirely — spike doc is the deferral record, no docs added anywhere)
- Read-only — modifies no plugin files

**Files to modify (1 new):**
- NEW `ai-agent-manager-plugin/docs/SPIKES/advisor.md`

**Skill references:** `quality-checklist`, `context7-lookup`

```yaml
provides:
  - {kind: "file", path: "ai-agent-manager-plugin/docs/SPIKES/advisor.md", name: "advisor.md"}
  - {kind: "symbol", path: "ai-agent-manager-plugin/docs/SPIKES/advisor.md", name: "Recommendation"}
requires: []
external_requires:
  - Anthropic Advisor tool documentation
  - Claude Code subagent tool propagation behavior
```

---

### S3 — Memory Tool Skill (No Spike Needed)
**Goal:** Create new skill documenting Memory Tool vs. file-based agent memory; add to SKILLS_INDEX.

**Acceptance criteria:**
- New file `ai-agent-manager-plugin/skills/memory-tool/SKILL.md` with proper YAML frontmatter (name: memory-tool, description, version: 1.0, lastUpdated: 2026-05-10)
- Sections: When to use Memory Tool vs file-based memory; tagging conventions; lifecycle (create on discovery, delete on contradiction); per-agent guidance for Red Team (vulnerability tags), QA Executor (infrastructure confirmed tags), Product Owner (stakeholder decision tags)
- `skills/SKILLS_INDEX.md` has a new entry under "Workflow / Orchestration" or appropriate category with directory, agent consumers, token estimate, version, lastUpdated

**Files to modify (1 new + 1 modify):**
- NEW `ai-agent-manager-plugin/skills/memory-tool/SKILL.md`
- `ai-agent-manager-plugin/skills/SKILLS_INDEX.md`

**Skill references:** `quality-checklist`

```yaml
provides:
  - {kind: "file", path: "ai-agent-manager-plugin/skills/memory-tool/SKILL.md", name: "SKILL.md"}
  - {kind: "symbol", path: "ai-agent-manager-plugin/skills/memory-tool/SKILL.md", name: "Memory Tool vs file-based"}
  - {kind: "symbol", path: "ai-agent-manager-plugin/skills/SKILLS_INDEX.md", name: "memory-tool"}
requires: []
external_requires: []
```

---

### S4 — Structured Outputs Guidance
**Goal:** Document Structured Outputs as a pattern for enforcing result schemas at the API level via `output_config.format`.

**Acceptance criteria:**
- `AGENT_GUIDELINES.md` has a new "Structured Outputs" section explaining: schemas in RESULT_SCHEMAS.md are the single source of truth; when building on the Claude API directly, enforce via `output_config.format` for guaranteed conformance; plugin hook validation is the runtime fallback
- `docs/RESULT_SCHEMAS.md` has a note at the top: "When using the Claude API directly, enforce via `output_config.format`. Plugin hook validation is the runtime fallback."

**Files to modify (2):**
- `AGENT_GUIDELINES.md`
- `ai-agent-manager-plugin/docs/RESULT_SCHEMAS.md`

**Skill references:** `quality-checklist`

```yaml
provides:
  - {kind: "symbol", path: "AGENT_GUIDELINES.md", name: "Structured Outputs"}
  - {kind: "symbol", path: "ai-agent-manager-plugin/docs/RESULT_SCHEMAS.md", name: "output_config.format"}
requires: []
external_requires:
  - Claude API output_config.format specification
```

---

### S5 — Conditional Implementation + Version Bump
**Goal:** Read spike outcomes from S1 and S2; conditionally implement Compaction (if Spike A GO) and `--advisor` flag (if Spike B GO); bump version to 12.1.0; refresh all docs.

**Acceptance criteria:**

**Conditional on Spike A (Compaction):**
- If S1's compaction.md has `Recommendation: GO`:
  - `hooks/hooks.json`: add `PreCompact` (type: command, archives `.supervisor/state.md`) and `PostCompact` (type: command, re-injects state JSON) hooks
  - `agents/context-keeper.md`: add `flush_for_compaction` operation
  - `skills/state-management/SKILL.md`: add "Compaction-Safe State Patterns" section
  - `skills/workflow-management/SKILL.md`: add "Compaction Behavior" section
- If S1's compaction.md has `Recommendation: NO-GO`: skip all compaction code; the spike doc itself is the deferral record

**Conditional on Spike B (Advisor) — exactly one of three branches:**
- **If `Recommendation: GO`:**
  - `commands/supervisor.md`: add `--advisor` flag documentation
  - `agents/supervisor.md`: parse `--advisor`, propagate `advisor_mode: true` in subtask briefs
  - `agents/worker.md`: add advisor consultation note for `advisor_mode: true` subtasks
  - `agents/code-reviewer.md`: add advisor consultation note before issuing FAIL/NEEDS_HUMAN
  - `docs/ARCHITECTURE_CONTRACTS.md` Cost Profiles table: add `advisor` row alongside `inherit` and `cheap`
  - `AGENT_GUIDELINES.md`: NOT modified (the plugin flag is the docs)
- **If `Recommendation: SDK-ONLY`:**
  - `AGENT_GUIDELINES.md`: add "Advisor Tool (SDK-only pattern)" section pointing to direct `/v1/messages` API usage
  - All other Advisor files: NOT modified (no plugin flag, no command doc, no cost profile row)
- **If `Recommendation: NO-GO`:**
  - No file modified for Advisor anywhere — the spike doc itself is the authoritative deferral record
  - `AGENT_GUIDELINES.md`: NOT modified
  - All Advisor-related files: NOT modified

**Unconditional:**
- `.claude-plugin/plugin.json`: version `12.0.0` → `12.1.0`; update description
- `CLAUDE.md`: version references; add notes for any features that landed (Memory Tool always; Compaction/Advisor conditionally)
- `README.md`: version references
- `.claude-plugin/README.md`: version references
- `.claude-plugin/marketplace.json`: version bump
- `commands/agent-help.md`: hook count and command count updates if changed
- `skills/SKILLS_INDEX.md`: refresh lastUpdated for all modified skills (memory-tool always; state-management/workflow-management if Compaction GO)
- Run `scripts/validate-version.sh` — exits 0
- Run `scripts/check-command-sync.sh` — exits 0

**Files to modify (conditional + unconditional, up to ~13):**
- Conditional Compaction: `ai-agent-manager-plugin/hooks/hooks.json`, `ai-agent-manager-plugin/agents/context-keeper.md`, `ai-agent-manager-plugin/skills/state-management/SKILL.md`, `ai-agent-manager-plugin/skills/workflow-management/SKILL.md`
- Conditional Advisor: `ai-agent-manager-plugin/commands/supervisor.md`, `ai-agent-manager-plugin/agents/supervisor.md`, `ai-agent-manager-plugin/agents/worker.md`, `ai-agent-manager-plugin/agents/code-reviewer.md`, `ai-agent-manager-plugin/docs/ARCHITECTURE_CONTRACTS.md`, `AGENT_GUIDELINES.md`
- Unconditional: `ai-agent-manager-plugin/.claude-plugin/plugin.json`, `CLAUDE.md`, `README.md`, `.claude-plugin/README.md`, `.claude-plugin/marketplace.json`, `ai-agent-manager-plugin/commands/agent-help.md`, `ai-agent-manager-plugin/skills/SKILLS_INDEX.md`

**Skill references:** `quality-checklist`

```yaml
provides:
  - {kind: "symbol", path: "ai-agent-manager-plugin/.claude-plugin/plugin.json", name: "12.1.0"}
  - {kind: "symbol", path: "CLAUDE.md", name: "v12.1.0"}
  - {kind: "symbol", path: ".claude-plugin/marketplace.json", name: "12.1.0"}
requires:
  - {from: "S1", kind: "symbol", path: "ai-agent-manager-plugin/docs/SPIKES/compaction.md", name: "Recommendation"}
  - {from: "S2", kind: "symbol", path: "ai-agent-manager-plugin/docs/SPIKES/advisor.md", name: "Recommendation"}
  - {from: "S3", kind: "file", path: "ai-agent-manager-plugin/skills/memory-tool/SKILL.md", name: "SKILL.md"}
  - {from: "S4", kind: "symbol", path: "AGENT_GUIDELINES.md", name: "Structured Outputs"}
external_requires: []
```

---

## Parallelism Analysis

```
Batch 1:  [S1] [S2] [S3] [S4]   — 4 workers (all read-only or new files; zero overlap)
Batch 2:  [S5]                  — 1 worker (blocked by all Batch 1 subtasks)
```

**LAUNCHABLE:** S1, S2, S3, S4
**BLOCKED by S1+S2+S3+S4:** S5

**Recommended workers:** 3 (matches repo Supervisor readiness guidance; one of S1/S2/S3/S4 will queue behind the first 3 — acceptable since S1/S2 are research-heavy and not all need to run simultaneously)
**Estimated total wall time:** ~2.5–3.5 hrs (Batch 1 limited by longest spike research)

---

## Skill References

| Subtask | Skills |
|---|---|
| S1 | `quality-checklist`, `context7-lookup` |
| S2 | `quality-checklist`, `context7-lookup` |
| S3 | `quality-checklist` |
| S4 | `quality-checklist` |
| S5 | `quality-checklist` |

---

## Risk Assessment

| Risk | Severity | Source | Mitigation |
|---|---|---|---|
| Both spikes resolve NO-GO → S5 becomes documentation-only | HIGH | Plan v12.1.0 framing | Acceptance criteria explicitly accept GO/NO-GO/SDK-ONLY outcomes; no blocker |
| S5 file list is conditional on spike outcomes — worker must read result docs first | MEDIUM | Conditional logic | S5 worker explicitly reads `docs/SPIKES/compaction.md` and `docs/SPIKES/advisor.md` Recommendation field before deciding which files to touch |
| Spike research uses `WebSearch`/`WebFetch` — may hit external API limits | LOW | Research-heavy work | Spikes can use `context7-lookup` for cached docs; fall back to local Claude Code documentation |
| Memory Tool skill may overlap with existing `.claude/agent-memory/` patterns | LOW | Pattern coexistence | Skill explicitly documents both as complementary, not competing |

---

## Configuration

```yaml
max_workers: 3
feature_branch: feature/v12.1-intelligence-upgrades
worktree_prefix: ai-agent-manager
cost_profile: inherit
```

---

## Handoff

```
/supervisor job: .supervisor/jobs/pending/2026-05-10-v12.1-intelligence-upgrades.md
```

Start a fresh Claude Code session with clean context and run the command above. **Pre-requisite:** PR #8 (v12.0.0) must be merged to main before starting.

---

## Outcome

- **Status:** completed
- **PR:** https://github.com/vikashruhilgit/ai-agent-manager/pull/9
- **Branch:** feature/v12.1-intelligence-upgrades
- **heal_loop_ran:** true
- **heal_decision:** PASS
- **heal_iterations:** 1
- **heal_remaining_issues:** 0
- **Spike A (Compaction):** NO-GO — deferred per docs/SPIKES/compaction.md
- **Spike B (Advisor):** SDK-ONLY — documented in AGENT_GUIDELINES.md
- **Subtask outcomes:** S1✓ S2✓ S3✓ S4✓ S5✓ (all completed)
- **Validation:** scripts/validate-version.sh exit 0; scripts/check-command-sync.sh exit 0
