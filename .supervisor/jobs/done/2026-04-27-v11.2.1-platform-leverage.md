# Supervisor Job: ai-agent-manager v11.2.1 — Platform Leverage

## Environment

- **Project:** ~/Documents/work/AI/ai-agent-manager
- **CLAUDE.md:** ✓ present (238 lines, fresh)
- **Git:** clean, branch: main
- **Worktrees:** 1 (no orphans)
- **GitHub CLI:** ✓ authenticated
- **Blockers:** 0 | **Warnings:** 0

## Task

Ship the smallest defensible v11.2 cut as **11.2.1** patch on top of the already-released 11.2.0 telemetry. Promote `scripts/` to plugin `bin/`, project Phase 4.5 self-heal outcomes into telemetry GitHub Issue labels, add count-drift CI guard (with pre-existing drift reconciliation), convert two hooks to JSONL output (using observed stdin payload).

**Plan reference:** `~/.claude/plans/let-create-aplan-ti-curious-moth.md` — full rationale, alternatives, risk analysis.

## Acceptance Criteria

- **AC1:** Given the plugin is installed downstream, when an agent's Bash tool runs `check-command-sync` or `validate-version` (no path) **from any cwd**, then both resolve from `ai-agent-manager-plugin/bin/` and exit with the same status as the legacy `scripts/` invocation. (Both bin/ scripts MUST use `git rev-parse --show-toplevel` for repo-root resolution — cwd-independence is mandatory, not optional.)
- **AC2:** `SUPERVISOR_RESULT { heal_loop_ran: true, heal_decision: ESCALATED }` → telemetry GitHub Issue carries `heal:escalated` label.
- **AC3:** `SUPERVISOR_RESULT { heal_loop_ran: false, status: failed, error: "...invariant violation..." }` → issue carries `heal:invariant-violation`.
- **AC4:** PR adding agent or skill without updating asserted counts → `aam-count-drift` exits 1, CI fails.
- **AC5:** WorktreeCreate hook event → `.supervisor/logs/worktrees.log` receives single JSON line with **at minimum** `{timestamp, worktree_path, session_id, cwd, hook_event_name}`. Optional fields (`branch`, `task_id`) emit `null` when absent from stdin payload — do not fabricate.
- **AC6:** StopFailure hook event → `.supervisor/logs/failures.log` receives single JSON line with `{timestamp, session_id, cwd, hook_event_name, error}`. (`agent_type` and `exit_code` are dropped — verified absent from real Claude Code StopFailure payload at `.supervisor/logs/failures.log`.)
- **AC7:** Existing CI invocations at `.github/workflows/ci.yml:18,21` continue to exit 0 on a clean tree post-wrapper.
- **AC8:** For each of the four new heal-* fixtures, an entry is added to `test-telemetry.sh`'s `expected_would_exit()` case statement (lines 192-224) covering all three consent states. Harness must emit `PASS` (not `SKIP`) for each fixture × state. Overall `bash ai-agent-manager-plugin/scripts/test-telemetry.sh` exits 0. (a) Hook validator at `hooks.json:54-65` continues to accept SUPERVISOR_RESULT schema unchanged. (b) Four label-derivation outcomes match the table in Subtask 2.
- **AC9:** End of Subtask 3, `aam-count-drift` exits 0 against the working tree (pre-existing drift reconciled within the same subtask).

## Subtask Structure

| # | Title | Files | Status | Skills |
|---|-------|-------|--------|--------|
| 1 | bin/ promotion + wrappers + validate-supervisor-jobs | 2 modify, 3 create | LAUNCHABLE | ci-cd, quality-checklist |
| 2 | Telemetry heal-outcome labels (code + fixtures + harness table) | 2 modify, 4 create | LAUNCHABLE | telemetry, monitoring-observability, unit-testing |
| 3 | Count drift reconciliation + aam-count-drift CI guard | 3 modify, 1 create | BLOCKED (by 1) | ci-cd, claude-md-validation |
| 4 | Hook output JSONL conversion (jq-based, observed payload) | 1 modify | LAUNCHABLE | monitoring-observability |
| 5 | Version bump to 11.2.1 + metadata sync + ALL doc updates | 5 modify | BLOCKED (by 1, 2, 3, 4) | claude-md-validation, quality-checklist |

### Subtask 1 — bin/ promotion

**Modify:**
- `scripts/check-command-sync.sh` → `exec "$(git rev-parse --show-toplevel)/ai-agent-manager-plugin/bin/check-command-sync" "$@"`
- `scripts/validate-version.sh` → `exec "$(git rev-parse --show-toplevel)/ai-agent-manager-plugin/bin/validate-version" "$@"`

**Create:**
- `ai-agent-manager-plugin/bin/check-command-sync` — body of current `scripts/check-command-sync.sh`. Replace `repo_root="$(cd "$(dirname "$0")/.." && pwd)"` (line 14) with `repo_root="$(git rev-parse --show-toplevel)"`. Set executable bit (`git update-index --chmod=+x`).
- `ai-agent-manager-plugin/bin/validate-version` — body of current `scripts/validate-version.sh`. **Mandatory** cwd-independent path resolution: `MARKETPLACE_JSON="$(git rev-parse --show-toplevel)/.claude-plugin/marketplace.json"`. Executable bit set.
- `ai-agent-manager-plugin/bin/validate-supervisor-jobs` — new bash. Lists `.supervisor/jobs/in-progress/*.md` older than `${AAM_JOB_STALE_HOURS:-24}h`; runs `git worktree list --porcelain`; reports orphan worktrees whose branch was deleted; exits 0 / 1; emits `KEY=VALUE` to stdout.

**Acceptance:** AC1, AC7. Test from 3 cwd contexts (repo root, deep subdirectory, /tmp via absolute path) — exit codes match legacy invocation in all three for both binaries.

**Confidence:** HIGH.

### Subtask 2 — Telemetry heal-outcome labels

**Modify:**
- `ai-agent-manager-plugin/scripts/send-telemetry-core.sh` — extend `SUPERVISOR_RESULT` handler. Label mapping:
  - `heal_loop_ran=false ∧ status=failed ∧ error~"invariant violation"` → `heal:invariant-violation`
  - `heal_loop_ran=false` (other) → `heal:skipped`
  - `heal_loop_ran=true ∧ heal_decision=PASS` → `heal:pass`
  - `heal_loop_ran=true ∧ heal_decision=ESCALATED` → `heal:escalated`
  - Append to existing `labels` array (script line 562); reuse existing `gh issue create --label` flow (lines 952-964).
- `ai-agent-manager-plugin/scripts/test-telemetry.sh` — extend `expected_would_exit()` case statement (lines 192-224) with entries for all four new heal-* fixtures across three consent states (no-consent, consent-with-repo, consent-no-repo). Each fixture × state must produce PASS, not SKIP/UNKNOWN.

**Create:**
- `ai-agent-manager-plugin/scripts/telemetry-fixtures/supervisor-heal-pass.json`
- `ai-agent-manager-plugin/scripts/telemetry-fixtures/supervisor-heal-escalated.json`
- `ai-agent-manager-plugin/scripts/telemetry-fixtures/supervisor-heal-invariant.json`
- `ai-agent-manager-plugin/scripts/telemetry-fixtures/supervisor-heal-skipped.json`

**Hard ACs:** AC2, AC3, AC8.

**Confidence:** HIGH.

### Subtask 3 — Count drift reconciliation + CI guard

**Modify:**
- `CLAUDE.md` — locate "Plugin Metadata" section via `grep -n "Plugin Metadata\|reusable skills\|quality gate hooks" CLAUDE.md`. Update skill count `47` → `48`. Reconcile hook count to a single canonical phrase: **"13 quality gate hooks (10 top-level in hooks.json + 3 per-agent frontmatter)"**.
- `ai-agent-manager-plugin/.claude-plugin/plugin.json` — description must contain a parser-stable phrase matching the regex `/(\d+) agents?, (\d+) (?:reusable )?skills?, (\d+) (?:quality gate )?hooks?/`. Use exact phrasing: **"12 agents, 48 skills, 13 hooks"** (no parentheticals — keep the parser anchor verbatim and surface the breakdown elsewhere in the description prose). Subtask 5 absorbs the broader description rewrite; Subtask 3 only touches the count-bearing phrase.
- `.github/workflows/ci.yml` — add CI step after existing `validate-version` and `check-command-sync` steps: invoke `aam-count-drift` via `bash "$(git rev-parse --show-toplevel)/ai-agent-manager-plugin/bin/aam-count-drift"`.

**Create:**
- `ai-agent-manager-plugin/bin/aam-count-drift` — bash (or python3, already in toolchain). Logic:
  1. Filesystem counts via globs: `agents/*.md` (excl. `*.template.md`), `skills/*/SKILL.md`, `commands/*.md`. Hook count = `jq '[.hooks | to_entries[] | .value | length] | add' hooks.json` PLUS grep for `hooks:` blocks in agent frontmatter.
  2. Asserted counts via regex on `plugin.json` description: `/(\d+) agents?, (\d+) (?:reusable )?skills?, (\d+) (?:quality gate )?hooks?/`. Document this regex as a comment in the script.
  3. Asserted counts via regex on CLAUDE.md "Plugin Metadata" section: same regex.
  4. Compare all three; exit 1 with diff output on mismatch.
- `ai-agent-manager-plugin/scripts/test-count-drift.sh` (small companion test) — fixture-driven: feed the script a known-mismatched plugin.json description string and assert exit 1; feed a matched string and assert exit 0.

**Acceptance:** AC4, AC9.

**Confidence:** MEDIUM (regex-on-prose is the brittlest part — anchor format is now pinned and unit-tested).

**Dependency:** Subtask 1 (bin/ infrastructure).

### Subtask 4 — Hook output JSONL (jq-based, observed payload)

**Modify:**
- `ai-agent-manager-plugin/hooks/hooks.json` — replace WorktreeCreate command at line 105 and StopFailure command at line 115 with `jq`-based JSON construction.

**WorktreeCreate command:**
```bash
INPUT=$(cat) && mkdir -p .supervisor/logs && \
echo "$INPUT" | jq -c \
  --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
  '{timestamp: $ts, worktree_path: (.worktree_path // null), branch: (.branch // null), task_id: (.task_id // null), session_id: (.session_id // null), cwd: (.cwd // null), hook_event_name: (.hook_event_name // null)}' \
  >> .supervisor/logs/worktrees.log
```

**StopFailure command:**
```bash
INPUT=$(cat) && mkdir -p .supervisor/logs && \
echo "$INPUT" | jq -c \
  --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
  '{timestamp: $ts, session_id: (.session_id // null), cwd: (.cwd // null), hook_event_name: (.hook_event_name // null), error: (.error // null)}' \
  >> .supervisor/logs/failures.log
```

(jq required by telemetry path — already a transitive dependency. CI runners ship jq.)

**Acceptance:** AC5, AC6.

**Confidence:** HIGH.

### Subtask 5 — Version bump to 11.2.1 + metadata + docs

**Modify:**
- `ai-agent-manager-plugin/.claude-plugin/plugin.json` — `version` `11.2.0` → `11.2.1`. Description rewrite, leading with parser-stable phrase: *"AI agents v11.2.1 — Platform leverage patch on top of v11.2.0 telemetry. Promotes scripts/ to plugin bin/ for downstream PATH access; projects Phase 4.5 self-heal outcomes into telemetry GitHub Issue labels (heal:pass / heal:escalated / heal:skipped / heal:invariant-violation); adds aam-count-drift CI guard with pre-existing drift reconciliation; converts WorktreeCheck and StopFailure hooks to jq-based JSONL output. 12 agents, 48 skills, 13 hooks. Builds on v11.2.0 (telemetry), v11.1.2 (Phase 4.5 invariant guard) and prior layers."*
- `.claude-plugin/marketplace.json` — `version` to `11.2.1`.
- `CLAUDE.md` — locate "Directory Structure" via `grep -n "Directory Structure"` and add `bin/` row. Add Common Pitfalls entry referencing `validate-supervisor-jobs` for orphan worktree diagnosis. (Do NOT mention `aam-cleanup-worktrees`.)
- `.claude-plugin/README.md` — mention `bin/` directory and the three executables shipped in v11.2.1.
- `ai-agent-manager-plugin/skills/telemetry/SKILL.md` — locate Labels section via `grep -n "Labels"`. Document new `heal:*` labels. Add Aggregation Queries subsection: `gh issue list --label heal:invariant-violation --state all`.
- `ai-agent-manager-plugin/docs/TELEMETRY.md` — locate via `grep -n "SUPERVISOR_RESULT\|heal"`. Brief mention of heal labels in schema-coverage section.

**Acceptance:** Implicit. `validate-version` (now wrapper) confirms parity. `aam-count-drift` confirms count integrity.

**Confidence:** HIGH.

**Dependency:** All prior subtasks.

## Parallelism Analysis

- **Batch 1 (parallel):** Subtask 1, Subtask 2, Subtask 4 — disjoint files. **Recommended workers: 3.**
- **Batch 2:** Subtask 3 (after 1).
- **Batch 3:** Subtask 5 (after all).

## Risk Assessment

| Severity | Risk | Mitigation |
|----------|------|------------|
| MEDIUM | bin/ PATH only docs-confirmed for Bash tool, not hook commands. | Hooks continue to use `${CLAUDE_PLUGIN_ROOT}` (works at hooks.json:15,29,63). |
| MEDIUM | aam-count-drift regex on plugin.json description is brittle. | Regex anchor pinned in Subtask 3. Unit fixture under `test-count-drift.sh`. |
| LOW | jq replaces python3 in WorktreeCreate/StopFailure hooks. | jq is already a transitive dep via telemetry. CI runners ship jq. |
| LOW | Wrappers break on Windows. | Out of scope. Document Windows unsupported in CLAUDE.md "Known Limitations". |

## Configuration

- **Branch:** `feature/v11.2.1-platform-leverage` (Supervisor MUST create; never work on main).
- **Heal iterations:** default (3).
- **Telemetry test:** maintainer should `export AI_AGENT_MANAGER_TELEMETRY_REPO=user/test-repo` and `/telemetry enable` before running Subtask 2 live verification.

## Handoff

```
/supervisor job: .supervisor/jobs/pending/2026-04-27-v11.2.1-platform-leverage.md
```
